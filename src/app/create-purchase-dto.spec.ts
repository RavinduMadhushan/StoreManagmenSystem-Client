import { CreatePurchaseDto } from './create-purchase-dto';

describe('CreatePurchaseDto', () => {
  it('should create an instance', () => {
    expect(new CreatePurchaseDto()).toBeTruthy();
  });
});
